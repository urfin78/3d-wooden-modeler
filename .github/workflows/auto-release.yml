name: Auto Release

on:
  workflow_dispatch:

# peter-evans/create-pull-request@v7 and the official actions/checkout|setup-node|upload-artifact@v5
# all still ship Node 20 bindings as of early 2026; GitHub forces JS actions to Node 24 on
# 2026-06-02 and removes Node 20 from runners on 2026-09-16. Opt in early to silence the
# deprecation warning and avoid breakage at the cutover.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Get latest tag
        id: latest_tag
        run: |
          latest=$(git tag --sort=-v:refname | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+' | head -1)
          echo "tag=${latest:-v0.0.0}" >> $GITHUB_OUTPUT

      - name: Determine version bump from commits
        id: bump
        run: |
          latest="${{ steps.latest_tag.outputs.tag }}"
          if git rev-parse "$latest" >/dev/null 2>&1; then
            commits=$(git log "$latest"..HEAD --pretty=format:"%s")
          else
            commits=$(git log --pretty=format:"%s")
          fi
          if [ -z "$commits" ]; then
            echo "bump=none" >> $GITHUB_OUTPUT
          elif echo "$commits" | grep -qE "^.+!:|BREAKING CHANGE"; then
            echo "bump=major" >> $GITHUB_OUTPUT
          elif echo "$commits" | grep -qE "^feat(\(.+\))?:"; then
            echo "bump=minor" >> $GITHUB_OUTPUT
          else
            echo "bump=patch" >> $GITHUB_OUTPUT
          fi

      - name: Calculate new version
        id: version
        if: steps.bump.outputs.bump != 'none'
        run: |
          version="${{ steps.latest_tag.outputs.tag }}"
          version="${version#v}"
          version="${version%%-*}"
          IFS='.' read -r major minor patch <<< "$version"
          major=${major:-0}; minor=${minor:-0}; patch=${patch:-0}
          case "${{ steps.bump.outputs.bump }}" in
            major) major=$((major+1)); minor=0; patch=0 ;;
            minor) minor=$((minor+1)); patch=0 ;;
            patch) patch=$((patch+1)) ;;
          esac
          echo "tag=v${major}.${minor}.${patch}" >> $GITHUB_OUTPUT

      - name: Generate release notes
        if: steps.bump.outputs.bump != 'none'
        run: |
          latest="${{ steps.latest_tag.outputs.tag }}"
          if git rev-parse "$latest" >/dev/null 2>&1; then
            git log "$latest"..HEAD --pretty=format:"- %s" \
              | grep -v "^- Merge" > release_notes.txt
          else
            git log --pretty=format:"- %s" \
              | grep -v "^- Merge" > release_notes.txt
          fi

      - name: Create GitHub Release
        if: steps.bump.outputs.bump != 'none'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ steps.version.outputs.tag }}" \
            --title "${{ steps.version.outputs.tag }}" \
            --notes-file release_notes.txt

      - name: Deploy to GitHub Pages
        if: steps.bump.outputs.bump != 'none'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp index.html /tmp/deploy_index.html
          if git ls-remote --exit-code --heads origin gh-pages > /dev/null; then
            git checkout gh-pages
          else
            git checkout --orphan gh-pages
            git rm -rf .
          fi
          cp /tmp/deploy_index.html index.html
          git add index.html
          git diff --staged --quiet || git commit -m "deploy: ${{ steps.version.outputs.tag }}"
          git push origin gh-pages
